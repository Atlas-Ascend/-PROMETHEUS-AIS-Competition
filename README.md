# PROMETHEUS AIS Competition Edition

**Repair. Prove. Remember. Evolve.**

PROMETHEUS V-1.1.1 is a self-proving AI engineering organism that converts one consequential software mission into an evidence-backed release decision.

> AI can claim completion. PROMETHEUS requires proof.

## Competition entry

- **Track:** Developer Tools
- **Live demonstration:** https://atlas-ascend.github.io/-PROMETHEUS-AIS-Competition/
- **Canonical public source:** `docs/`
- **Compatibility entrypoint:** `/index.html` loads the same `docs/` runtime
- **Executable platform:** Python 3.10 or newer
- **License:** PROMETHEUS Competition Evaluation License v1.0

## Final public mission

`PROM-COMP-EDGE-001` is one uninterrupted three-minute orchestration run against a compound failure storm:

- conflicting requirements
- malformed and poisoned input
- duplicate execution
- protected-data exposure
- dependency loss
- corrupted evidence
- rollback pressure

No new organs are introduced for the demonstration.

## Existing system fabric

```text
PROMETHEUS
→ AIS-Ω
→ Adversarial Twin
→ SECA
→ HYDRA
→ ProofGrid
→ Capability Genome
→ Build Truth
```

The same eight systems admit the mission, evaluate candidate routes, reproduce the cascading failures, deny unsupported promotion, repair behavior, verify evidence, preserve the successful capability and authorize release.

## Public interface

The public site is intentionally limited to the elite demonstration:

- one ignition control
- one 180-second mission
- eight-system handoff fabric
- seven live edge-condition states
- synchronized mission telemetry
- evidence closure
- final Build Truth verdict
- sealed ServerForge publication envelope

The prior case-study essay, judge controls, submission instructions, methodology panels, limitations register and administrative presentation surface are retired from the public runtime.

## Canonical paths

```text
/index.html                         compatibility public entrypoint
/docs/index.html                    canonical elite demonstration
/docs/assets/elite-demo.css         canonical visual system
/docs/assets/elite-demo.js          canonical three-minute runtime
/docs/proofgrid/FINAL_RECEIPT.json  public ProofGrid receipt
/docs/data/serverforge-publication-envelope.json
.github/workflows/deploy-pages.yml  authoritative Pages deployment
BUILD_TRUTH.md                      current release authority
```

## Run and verify locally

```bash
python -m pip install -e .
python -m unittest discover -s tests -v
python -m prometheus.cli doctor --json
python -m prometheus.cli demo reset
python -m prometheus.cli demo competition
python -m prometheus.cli receipt verify artifacts/competition-demo/FINAL_RECEIPT.json
python -m prometheus.cli contest claims-verify
python -m prometheus.cli contest evidence-verify
python -m prometheus.cli contest submission-check
```

## Public boundary

The competition edition excludes credentials, private prompts, private machine state, unpublished repositories, proprietary routing details and patent-sensitive mechanisms. ServerForge receives only the sanitized publication envelope and proof references.

## Release discipline

Architecture is frozen. The final lane is limited to verified blocker repair, deployment convergence, truth alignment and submission delivery.
