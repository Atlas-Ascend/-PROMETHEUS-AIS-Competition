# ServerForge public handoff

This directory contains the credential-free publication envelope consumed by the existing EDEN-local ServerForge route.

The public repository never stores Discord bot tokens, webhooks, private prompts, machine paths, or patent-sensitive implementation details. EDEN remains the private transport authority and must return an external receipt after publication.
