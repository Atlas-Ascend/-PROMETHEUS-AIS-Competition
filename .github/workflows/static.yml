name: Deploy PROMETHEUS Competition Website

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/static.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate website, proof theater, telemetry and public evidence
        run: |
          set -euo pipefail
          test -s docs/index.html
          test -s docs/assets/styles.css
          test -s docs/assets/demo.css
          test -s docs/assets/app.js
          test -s docs/data/demo.json
          test -s docs/screenshots/prometheus-command-center.svg
          test -f docs/.nojekyll
          node --check docs/assets/app.js
          grep -q 'id="start-demo"' docs/index.html
          grep -q 'id="replay"' docs/index.html
          grep -q 'id="proof-theater"' docs/index.html
          grep -q 'id="theater-launch"' docs/index.html
          grep -q 'function runTheater()' docs/assets/app.js
          grep -q 'function animatePacket' docs/assets/app.js
          grep -q 'function startTelemetry' docs/assets/app.js
          grep -q 'function setGauge' docs/assets/app.js
          grep -q 'packetMove' docs/assets/demo.css
          grep -q 'telemetry-line' docs/assets/demo.css
          python -m json.tool docs/data/demo.json > /dev/null
          python -m json.tool docs/proofgrid/FINAL_RECEIPT.json > /dev/null
          python -m json.tool docs/proofgrid/HYDRA_REPAIR_RECEIPT.json > /dev/null
          python -m json.tool docs/genomes/CAPABILITY_GENOME.json > /dev/null
          python -m json.tool docs/genomes/CAPABILITY_GENOME_REUSE.json > /dev/null

  deploy:
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload PROMETHEUS static website
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
