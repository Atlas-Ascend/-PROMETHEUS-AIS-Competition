name: RETIRED - Legacy live-site verifier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired legacy Operator Console verifier. Locked release target is the 2:45 PROMETHEUS demo."
